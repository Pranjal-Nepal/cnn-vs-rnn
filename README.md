# cnn-vs-rnn
This report compares a spatial CNN against a sequential RNN (LSTM) on MNIST. The CNN extracts geometric shapes from the entire 2D image at once, while the RNN scans the pixel grid row-by-row across 28 steps. The CNN achieved 98.82% accuracy and the RNN scored 98.69%, giving the CNN a tiny edge.
